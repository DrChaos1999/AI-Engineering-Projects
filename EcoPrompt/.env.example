# Copy to .env and fill in. The code reads OPENAI_API_KEY from the environment.
# Without a key (or without the openai package) the pipeline runs in MOCK mode:
# deterministic, offline, zero-cost -- useful for development and CI.
OPENAI_API_KEY=sk-...
